# ci-actions

Shared GitHub Actions for Post-Code-Labs repositories.

Each action lives in its own top-level directory with an `action.yml`. Reference
it pinned to a commit SHA:

```yaml
uses: Post-Code-Labs/ci-actions/<action>@<sha>
```

Conventions: see [AGENTS.md](AGENTS.md).
